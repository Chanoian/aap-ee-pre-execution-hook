---
- name: Ping Localhost
  hosts: localhost
  connection: local

  tasks:
    - name: Sleep
      ansible.builtin.pause:
        seconds: 90
    - name: Test connection
      ansible.builtin.ping:
...
